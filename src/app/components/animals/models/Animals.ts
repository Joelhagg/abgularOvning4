export class Animals {
    animal: string;
    size: string;
    age: number;
    isFed: boolean;

    constructor(animal: string, size: string, age: number, isFed: boolean) {
        this.animal = animal,
        this.size = size,
        this.age = age,
        this.isFed = isFed
    }
}